import { Button } from "@nextui-org/react";
import "./home.css";

const Home = () => {
  return (
    <div>
      <h1 className="text-secondary">Home Page</h1>
      <Button className="btn-ghost px-10">Add</Button>
    </div>
  );
};

export default Home;
